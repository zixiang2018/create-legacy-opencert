import { Document, SchematisedDocument, SignedDocument } from "../privacy";
export declare type SignatureProofAlgorithm = "SHA3MerkleProof";
export interface Signature {
    type: SignatureProofAlgorithm;
    targetHash: string;
    proof: string[];
    merkleRoot: string;
}
export declare const sign: (document: SchematisedDocument, batch?: string[] | undefined) => SignedDocument;
export declare const verify: (document: any) => document is Document;
