import { Document, SchematisedDocument } from "../privacy";
export interface Schema {
    id?: string;
    $id: string;
    $schema?: string;
    type?: string;
    properties?: any;
    required?: string[];
    additionalProperties?: boolean;
}
export declare const addSchema: (schema: Schema) => void;
export declare const validate: (document: Document, schema?: Schema | undefined) => document is SchematisedDocument;
